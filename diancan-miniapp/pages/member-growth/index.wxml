<view class="page">
  <app-navbar title="成长明细" />
  <view class="tips-card">
    <view class="tips-title">成长值说明</view>
    <view class="tips-desc">成长值用于会员升级，一期规则为消费后自动累计，不参与抵扣。</view>
  </view>

  <view class="empty" wx:if="{{!loading && records.length === 0}}">
    <view class="empty-icon">📈</view>
    <view class="empty-title">暂无成长记录</view>
    <view class="empty-desc">支付成功后累计的成长值会显示在这里</view>
  </view>

  <view class="record-list" wx:else>
    <view class="record-card" wx:for="{{records}}" wx:key="id">
      <view class="record-head">
        <view class="record-title">{{item.bizType || '成长值变动'}}</view>
        <view class="record-value {{item.changeAmount >= 0 ? 'is-plus' : 'is-minus'}}">
          {{item.changeAmount >= 0 ? '+' : ''}}{{item.changeAmount}}
        </view>
      </view>
      <view class="record-meta">
        <text>当前成长值 {{item.growthAfter || 0}}</text>
        <text>{{item.createTime || ''}}</text>
      </view>
      <view class="record-remark" wx:if="{{item.remark}}">{{item.remark}}</view>
    </view>

    <view class="list-footer" wx:if="{{records.length > 0}}">
      <view class="list-footer__text" wx:if="{{loadingMore}}">正在加载更多...</view>
      <view class="list-footer__text" wx:elif="{{finished}}">已经到底了</view>
      <view class="list-footer__text" wx:else>上拉继续加载</view>
    </view>
  </view>
</view>
